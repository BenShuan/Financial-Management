# Agent onboarding

1. **Domain (what to build):** `docs/agent/README.md` — read `00` → `06` for scope, entities, workflows, and agent conventions.
2. **Engineering (how we build):** `docs/engineering/00-tech-stack.md` — monorepo layout, stack choices, hosting, CI.
3. **Cursor:** Project rules live in `.cursor/rules/*.mdc`; optional skills in `.cursor/skills/*/SKILL.md` for domain, API, web, and CI tasks.

When domain docs and code disagree, fix docs or code in the same change set (see `docs/agent/06-agent-conventions.md`).
