# API base URL, baked in at build time (Vite). No trailing slash.
# Production (set in Vercel): the Render service URL,
# e.g. https://financial-management-api.onrender.com
VITE_API_ORIGIN=http://localhost:8787
