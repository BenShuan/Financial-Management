# `@financial-management/api-client`

Optional typed HTTP helpers for the web app (or other clients). Uses Zod + types from `@financial-management/shared` so responses stay aligned with the API.

For v1 this is a small hand-written surface. You can later swap the implementation for code generated from the OpenAPI document served by `apps/api` (`/openapi.json`).
